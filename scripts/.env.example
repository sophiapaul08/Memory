# LLM Council Configuration - OpenCode CLI Only
# All models use OpenCode CLI for worktree-based code generation

# Council Members - Comma-separated list of provider/model identifiers
# Format: opencode/provider/model (or just provider/model, defaults to opencode)
# Note: Use models with tool/function calling support for file editing
#
# Recommended free models with tool support:
#   - qwen/qwen3-coder:free (coding specialist, 262k context)
#   - google/gemini-2.0-flash-exp:free (1M context)
#   - meta-llama/llama-3.3-70b-instruct:free (large model)
#   - mistralai/mistral-small-3.1-24b-instruct:free (balanced)
#   - qwen/qwen3-235b-a22b:free (very large)
#
# Example with free models:
COUNCIL_MODELS=opencode/openrouter/qwen/qwen3-coder:free,opencode/openrouter/google/gemini-2.0-flash-exp:free,opencode/openrouter/meta-llama/llama-3.3-70b-instruct:free

# Chairman Model - Synthesizes final response (larger model recommended)
CHAIRMAN_MODEL=opencode/openrouter/qwen/qwen3-235b-a22b:free

# Title Generation Model - Generates short conversation titles (fast model preferred)
TITLE_MODEL=opencode/openrouter/mistralai/mistral-small-3.1-24b-instruct:free

# Dashboard Settings
# Time in seconds to show dashboard after completion before closing
DASHBOARD_TIMEOUT=5
# Dashboard refresh rate in Hz (updates per second). Higher = smoother but more CPU
DASHBOARD_REFRESH_RATE=10
